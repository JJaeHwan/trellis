/**
 * {{namePascal}}Service — 비즈니스 로직 계층.
 * Repository / 외부 API 등을 inject 받아 도메인 작업 수행.
 */
export class {{namePascal}}Service {
  constructor() {
    // TODO: inject dependencies (Repository, API client, etc.)
  }

  // TODO: add your service methods
}
