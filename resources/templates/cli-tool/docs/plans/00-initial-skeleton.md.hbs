# P0 — Initial Skeleton

> {{projectName}} 의 첫 작업 — 빌드 가능한 hello 커맨드까지.

---

## 1. 목표

- `npm install && npm run build` 통과
- `node dist/cmd/index.js hello` → `Hello from {{projectName}}` 출력
- CI 첫 실행 초록

---

## 2. 작업

- [ ] `npm install` — 에러 없이 완료
- [ ] `npm run typecheck` — 0건
- [ ] `npm run lint` — 0건
- [ ] `npm run test` — 통과
- [ ] `npm run dep:check` — 0 위반
- [ ] `npm run build` — `dist/cmd/index.js` 생성
- [ ] `node dist/cmd/index.js hello` — 정확한 문자열 출력
- [ ] GitHub 저장소 생성 + `git init && git push`
- [ ] CI 워크플로우 실행 + 초록 확인
- [ ] README 의 "상태" 섹션 갱신

---

## 3. 완료 기준

- [ ] 위 모든 작업 완료
- [ ] 후속 플랜 `docs/plans/01-*.md` 초안 작성
- [ ] 이 파일을 `docs/plans/completed/00-initial-skeleton.md` 로 이동

---

생성: trellis {{trellisVersion}}
