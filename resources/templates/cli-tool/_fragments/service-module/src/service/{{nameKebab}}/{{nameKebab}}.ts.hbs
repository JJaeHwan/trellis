import type { {{namePascal}}ServiceInput, {{namePascal}}ServiceResult } from "./types.js";

/**
 * {{namePascal}}Service — TODO: 비즈니스 로직 작성.
 */
export class {{namePascal}}Service {
  run(input: {{namePascal}}ServiceInput): {{namePascal}}ServiceResult {
    return { ok: true, value: input.value };
  }
}
