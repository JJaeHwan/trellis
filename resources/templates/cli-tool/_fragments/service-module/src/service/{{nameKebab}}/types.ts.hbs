/**
 * {{namePascal}}Service 의 입력/결과 타입.
 *
 * 구현 세부는 {{nameKebab}}.ts 참조.
 */

export interface {{namePascal}}ServiceInput {
  readonly value: string;
}

export interface {{namePascal}}ServiceResult {
  readonly ok: boolean;
  readonly value: string;
}
