{
  "description": "AI RAG page component — React client component with search/chat UI (document selection, question input, streaming answer). All required packages (next, react, react-dom) are already declared in the full-body package.json.",
  "patches": [
    {
      "file": "src/lib/nav-items.ts",
      "slot": "nav-items",
      "entryKey": "/{{nameKebab}}",
      "content": "{ label: \"{{namePascal}}\", href: \"/{{nameKebab}}\" },"
    }
  ]
}
